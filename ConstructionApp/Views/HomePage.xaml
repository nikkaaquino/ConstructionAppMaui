<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:cv="clr-namespace:Camera.MAUI;assembly=Camera.MAUI"
             x:Class="ConstructionApp.Pages.HomePage"
             Title="HomePage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="View Captured Photos" Clicked="OnPhotoListClicked"/>
    </ContentPage.ToolbarItems>

    <ScrollView>

        <VerticalStackLayout
         Spacing="25"
        Padding="30,0"
        VerticalOptions="Fill"
        HorizontalOptions="Fill">

            <cv:CameraView x:Name="cameraView" WidthRequest="300" HeightRequest="300"
                       CamerasLoaded="cameraView_CamerasLoaded"/>

            <Button Text="Capture" Clicked="Button_Clicked"/>

            <Image  x:Name="myImage" WidthRequest="200" HeightRequest="200"/>

            <Button Text="Retake" Clicked="Retake_Clicked"/>
            <Button Text="Save Image" Clicked="Save_Clicked"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>